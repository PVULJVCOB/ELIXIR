{
  "name": "elixir",
  "private": true,
  "version": "1.0.0",
  "description": "Static site for SideRun demo",
  "scripts": {
    "format": "prettier --write .",
    "lint:css": "stylelint \"styles/**/*.css\"",
    "lint:css:fix": "stylelint \"styles/**/*.css\" --fix"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "stylelint": "^16.8.1",
    "stylelint-config-recommended": "^14.0.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
